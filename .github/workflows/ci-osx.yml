name: CI on Mac

on: [push]

jobs:
  build:

    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2
    - name: Install AORSA dependencies
      run: sudo brew install open-mpi scalapack pgplot
    - name: Build
      run: make
    - name: Test
      run: ctest
